<Page x:Class="AHIFventory.OrderPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:AHIFventory"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="OrderPage">

    <Grid Background="LightGray">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Row="0" Background="WhiteSmoke" CornerRadius="10" Margin="5">
            <TextBlock Text="Orders" TextAlignment="Center" />
        </Border>

        <Border Grid.Row="1" Background="LightGray" CornerRadius="10" Margin="5">
            <ScrollViewer>
                <StackPanel x:Name="OrdersStackPanel">
                    <local:OrderUserControl Margin="5" />
                    <local:OrderUserControl Margin="5" />
                    <local:OrderUserControl Margin="5" />
                    <local:OrderUserControl Margin="5" />
                </StackPanel>
            </ScrollViewer>
        </Border>

    </Grid>
</Page>
